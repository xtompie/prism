---
name: name-the-failure-mode
when: creative work (UI, text, name) — cliché looms
when_not: a purely mechanical task with no "taste"
---
Name the failure mode you're avoiding outright ("not generic AI-slop", "not another landing page"), not just the positive goal — a forbidden direction steers harder than "do it well". Then ban the SECOND-order escape too: watch where it flees once the obvious default is gone (Inter → Space Grotesk, purple gradient → dark indigo) and forbid that as well.
