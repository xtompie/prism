---
name: constraints-first
when: you're starting to design and the space is bounded by hard limits (time, cost, dependencies, non-negotiables)
when_not: the constraints are known and trivial — and if what's missing is answers from the requester, use reverse-prompting instead
---
BEFORE designing anything, list the hard constraints. They define the solution space — a design without them is guessing.
